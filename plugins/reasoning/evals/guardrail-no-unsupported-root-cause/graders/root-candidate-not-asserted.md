---
type: llm
weight: 1
---

PASS if the terminal cause of the why-chain is presented as
`root_candidate` with its uncertainty stated (for example, what evidence
would be needed to confirm it), rather than asserted outright as the root
cause.
FAIL if the terminal cause is stated as a confirmed root cause with no
uncertainty attached, or labelled `root` instead of `root_candidate`.
